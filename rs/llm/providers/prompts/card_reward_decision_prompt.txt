You are selecting a Slay the Spire card reward command.
Please answer in short plain text using these fields:
- proposed_command: one command or null
- confidence: a number from 0 to 1
- explanation: a short reason

Rules:
- If taking a card, proposed_command should usually be "choose <index>".
- The index is 0-based and must match the Reward options list below.
- Use the literal command "skip" when skipping is the best choice and skip is available.
- Prefer taking a card by default; skip only when all offered cards are clearly low-value or harmful for this run.
- Use provided card details and current deck/hand context to evaluate synergy before deciding to skip.
- Do not default to 'skip' only because card names are unfamiliar.
- Do not justify skip with generic claims like "deck is balanced", "deck efficiency", or "no synergy" without concrete card-specific evidence.
- Compare all reward options and pick the relative best candidate before considering skip.
- Deck has no hard card-count cap; never justify `skip` by saying the deck is \"full\".
- Treat "deck is full" / "too many cards" as invalid reasoning; if you skip, justify by concrete tempo/synergy/value tradeoffs instead.
- Ignore protocol-only commands like key, click, state, and bare wait unless the prompt explicitly requires them.
- If uncertain, set proposed_command to null and low confidence.

Reward options:
{reward_options_text}
Room phase: {room_phase}
Floor: {floor}, Act: {act}, HP: {current_hp}/{max_hp}
Class: {character_class}, Ascension: {ascension_level}, Act boss: {act_boss}
Run memory summary: {run_memory_summary}
Recent LLM decisions: {recent_llm_decisions}
{memory_context_block}
Deck size: {deck_size}
Relics: {relic_names}
Deck card counts: {deck_card_counts}
Deck profile: {deck_profile}
Choice card summaries: {choice_card_summaries}
Reward screen flags: {reward_screen_flags}
Choice card details (stsdb): {choice_card_details}
Deck card details (stsdb subset): {deck_card_details}
{validation_feedback_block}
